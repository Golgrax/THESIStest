#This is where the necessary libraries will be inputted for easy access/installation.
#To install all of the libraries at once, enter "pip install -r requirements.txt" in the terminal.

kivy
kivymd
requests
numpy
pillow
opencv
tflite-runtime
pytz
matplotlib
